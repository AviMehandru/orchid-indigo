# Meson's build directory (linux-gtk). Every byte of it is regenerated by
# `meson compile`.
build/
builddir/

# Xcode (macos), once it exists.
DerivedData/
*.xcuserstate
xcuserdata/

# MSBuild (windows), once it exists.
bin/
obj/
*.user

.DS_Store
